{% extends 'base.html.twig' %}

{% block title %}
    {{ "Édition de #{record.id}" }}
{% endblock %}

{% block main %}
    <div class="main-content">
        <h1>{{ "Édition de #{record.id}"}}</h1>
        {{ include('record_animal/_form.html.twig', {
            label_submit: "modifier",
            with_context: false,
            form: form
        })
        }}
    </div>
{% endblock %}
