{% extends 'base.html.twig' %}

{% block title %}
    {{ "Édition de #{animal.name}" }}
{% endblock %}

{% block body %}
    <h1>{{  "Édition de #{animal.name}"}}</h1>
    {{ include('animal/_form.html.twig', {
        label_submit: "modifier",
        with_context: false,
        form: formulaire
    })
    }}
{% endblock %}
