{{ form_start(form) }}

    {{ form_row(form.name, {'label': 'name'}) }}
    {{ form_row(form.note, {'label': 'note'}) }}
    {{ form_row(form.race, {'label': 'race'}) }}
    {{ form_row(form.birthday, {'label': 'birthday'}) }}
    {{ form_row(form.gender, {'label': 'gender'}) }}
    {{ form_row(form.isDomestic, {'label': 'isDomestic'}) }}
    {{ form_row(form.photo, {'label': 'photo'}) }}
<input type="submit" class="btn btn-primary" value="{{ label_submit }}">
{{ form_end(form) }}