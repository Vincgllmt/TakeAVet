{% extends 'base.html.twig' %}

{% block title %}
    {{ "Suppression de #{animal.name}" }}
{% endblock %}

{% block body %}
    <h1>{{ "Suppression de #{animal.name}" }}</h1>
    <p> êtes vous sur de vouloir supprimer cet Animal ?</p>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}
